# Examples

Examples in this folder are intended as small smoke tests after MCP is running.

For larger FPC layout stress batches, prefer a separate manifest-driven runner
that writes outputs under `C:\abaqus-mcp\jobs\...` and then aggregates CSV/JSON
results.

